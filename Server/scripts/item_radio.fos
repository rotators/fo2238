//
// FOnline: 2238
// Rotators
//
// item_radio.fos
//

#include "_macros.fos"

void item_init(Item& radio, bool firstTime)
{
    if(firstTime)
    {
        radio.RadioChannel = Random(0, 10);
        radio.RadioFlags = RADIO_DISABLE_RECV;
        radio.Update();
    }
}
