//
// FOnline: 2238
// Rotators
//
// random_quests_h.fos
//

// Randomly generated quests

// todo: fill with more
#define TYPE_FETCH            (0) // Simple fetch, "bring 10 scorpion tails".
#define TYPE_FETCH_SPECIAL    (1) // Fetch special item, for example a lost watch in a cave.
#define TYPE_STEAL            (2) // Steal item(s) from random npc.
#define TYPE_KILL_NPC         (3) // Kill specific NPC
#define TYPE_KILL_PLAYER      (4) // Kill a player
